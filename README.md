# containerized_model_endpoint_example
Containerized simple ML model using FastAPI, Docker so could easily deployed

Steps
-----

 - Run the live server
 
   go to `./endpoint/main.py` and then `$ uvicorn main:app --reload` 
